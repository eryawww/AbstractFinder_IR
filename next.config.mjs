/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['via.placeholder.com'], // Add allowed external image domains here
    },
};

export default nextConfig;
